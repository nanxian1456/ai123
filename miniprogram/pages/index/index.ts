import { request } from "../../utils/api";
interface Contact { id: number; name: string; organization: string; position: string; city: string; initial?: string; }
interface Dashboard { contactCount: number; organizationCount: number; cityCount: number; recentContacts: Contact[]; }
Page({ data: { dashboard: null as Dashboard | null, loading: true, loadError: "" }, onShow() { this.loadDashboard(); }, async loadDashboard() { this.setData({ loading: true, loadError: "" }); try { const dashboard = await request<Dashboard>("/dashboard"); dashboard.recentContacts = (dashboard.recentContacts || []).map(contact => ({ ...contact, initial: contact.name.charAt(0) })); this.setData({ dashboard, loading: false }); } catch { this.setData({ loading: false, loadError: "后端未连接，请先启动 Java 服务" }); } }, goAdd() { wx.navigateTo({ url: "/pages/contact-form/index" }); }, goContacts() { wx.switchTab({ url: "/pages/contacts/index" }); }, goMap() { wx.switchTab({ url: "/pages/map/index" }); }, goGraph() { wx.navigateTo({ url: "/pages/knowledge-graph/index" }); }, goAi() { wx.navigateTo({ url: "/pages/contact-form/index?mode=ai" }); }, openContact(event: any) { wx.navigateTo({ url: `/pages/contact-detail/index?id=${event.currentTarget.dataset.id}` }); } });

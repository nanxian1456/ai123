import { request, showError } from "../../utils/api";
Page({ data: { isEdit: false, saving: false, form: { nickname: "", organization: "", position: "", city: "", bio: "" } }, onLoad(query: Record<string, string>) { const isEdit = query.mode === "edit"; this.setData({ isEdit }); if (isEdit) this.load(); }, async load() { try { const profile = await request<any>("/me"); this.setData({ form: { nickname: profile.nickname || "", organization: profile.organization || "", position: profile.position || "", city: profile.city || "", bio: profile.bio || "" } }); } catch (error) { showError(error); } }, input(event: any) { this.setData({ [`form.${event.currentTarget.dataset.field}`]: event.detail.value }); }, async save() { if (!this.data.form.nickname.trim()) return wx.showToast({ title: "请填写昵称", icon: "none" }); this.setData({ saving: true }); try { await request("/me", "PATCH", this.data.form); wx.showToast({ title: "资料已保存", icon: "success" }); setTimeout(() => this.data.isEdit ? wx.navigateBack() : wx.switchTab({ url: "/pages/index/index" }), 400); } catch (error) { showError(error); } finally { this.setData({ saving: false }); } } });
